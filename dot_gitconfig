[user]
    name = Orion Yeung
    email = 11580988+orionyeung001@users.noreply.github.com

[init]
    defaultBranch = main

[core]
    editor = hx
    pager = delta --pager='less -R' --line-numbers --side-by-side --wrap-max-lines=10
    excludesFile = /home/orion/.config/git/exclude

[path]
    followTags = true

[include]
    path = /home/orion/.config/delta/themes.gitconfig
[delta]
    features = colibri

[delta "interactive"]
    keep-plus-minus-markers = false

[delta "decorations"]
    commit-decoration-style = blue ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = blue box
    hunk-header-file-style = red
    hunk-header-line-number-style = "#067a00"
    hunk-header-style = file line-number syntax
[alias]
    lazy = !$GOPATH/bin/lazygit
    s = !$GOPATH/bin/lazygit
    initempty = !git init && git commit --allow-empty -m \"Init commit\"
    co = checkout
    dft = !difft
[credential "https://github.com"]
    helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
    helper = !/usr/bin/gh auth git-credential
